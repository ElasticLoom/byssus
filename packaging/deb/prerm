#!/bin/sh
set -e

case "$1" in
remove)
    if [ -d /run/systemd/system ]; then
        # Stopping does not remove mounts; see "Removing Byssus" in
        # /usr/share/doc/byssus/OPERATIONS.md.
        systemctl disable --now byssusd.service >/dev/null 2>&1 || true
    fi
    ;;
esac

exit 0
